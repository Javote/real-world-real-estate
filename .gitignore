# Node / TypeScript
node_modules/
dist/
.output/
.nitro/
.tanstack/
*.tsbuildinfo

# Secretos y datos locales
.env
.env.*
!.env.example

# SQLite de desarrollo (Drizzle + @libsql/client, D-048). Rutas relativas a
# packages/api/ — ya no a packages/api/prisma/, que dejó de existir.
packages/api/dev.db
packages/api/dev.db-journal
packages/api/dev.db-wal
packages/api/dev.db-shm
packages/api/test.db
packages/api/test.db-journal
packages/api/test.db-wal
packages/api/test.db-shm

# Archivos subidos por usuarios (runtime, no código)
packages/api/uploads/*
!packages/api/uploads/.gitkeep
packages/api/test-uploads/

# Artefactos de compilación de Aiken (el blueprint plutus.json SÍ se commitea)
contracts/build/

# Tests / cobertura
coverage/
playwright-report/
test-results/

# Sistema operativo / editor
.DS_Store
.idea/
.vscode/

# Config local de Claude Code
.claude/settings.local.json

# Artefactos de la suite E2E (capturas, videos, traces, reporte)
apps/web/e2e/.artifacts/
