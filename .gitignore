# Node / TypeScript
node_modules/
dist/
.output/
.nitro/
.tanstack/
*.tsbuildinfo

# Secretos y datos locales
.env
.env.*
!.env.example

# SQLite de desarrollo (Kysely sobre @libsql/client, D-049).
apps/api/dev.db
apps/api/dev.db-journal
apps/api/dev.db-wal
apps/api/dev.db-shm
# Bases de la suite: una por archivo de test, más la plantilla (SPEC-015 §1).
apps/api/.data/

# Archivos subidos por usuarios (runtime, no código). El `/uploads` de la raíz
# no debería existir nunca; está por si alguien arranca la API desde ahí.
/uploads/
apps/api/uploads/*
!apps/api/uploads/.gitkeep
apps/api/test-uploads/

# Artefactos de compilación de Aiken (el blueprint plutus.json SÍ se commitea)
contracts/build/

# Tests / cobertura
coverage/
playwright-report/
test-results/

# Sistema operativo / editor
.DS_Store
.idea/
.vscode/

# Config local de Claude Code
.claude/settings.local.json

# Artefactos de la suite E2E (capturas, videos, traces, reporte)
apps/web/e2e/.artifacts/
