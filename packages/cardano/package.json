{
  "name": "@plataforma/cardano",
  "version": "0.0.0",
  "private": true,
  "description": "AnchorPort: la cadena detrás de una interfaz propia, con modo real y simulado (D-014, SPEC-013)",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepare": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@plataforma/shared": "workspace:^"
  },
  "devDependencies": {
    "@types/node": "^22.13.14",
    "typescript": "^6.0.2",
    "vitest": "^4.1.5"
  }
}
