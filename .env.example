# ── Hoy (D-016) ────────────────────────────────────────────────────────────
# La API lee su .env desde packages/api/ — el ejemplo vigente está en
# packages/api/.env.example (DATABASE_URL sqlite, JWT_SECRET, PORT, UPLOAD_DIR).
#   cp packages/api/.env.example packages/api/.env

# ── Destino (se activan al integrar cada pieza; ver DECISIONS) ─────────────
# Base de datos al desplegar (trigger de D-016)
# DATABASE_URL=postgres://app:app@localhost:5432/plataforma

# Storage de evidencia S3-compatible (D-011; MinIO local / R2 prod)
# S3_ENDPOINT=http://localhost:9000
# S3_ACCESS_KEY=minio
# S3_SECRET_KEY=minio12345
# S3_BUCKET=evidence

# Cardano (D-013: SIEMPRE Preprod hasta aprobación de gobernanza; D-014: ANCHOR_MODE)
# CARDANO_NETWORK=Preprod
# ANCHOR_MODE=simulated
# BLOCKFROST_API_KEY=preprod_xxx
# SERVICE_WALLET_SEED=palabra1 palabra2 ... palabra24
# EXPLORER_BASE=https://preprod.cardanoscan.io
# ANCHOR_METADATA_LABEL=1904
